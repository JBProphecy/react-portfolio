////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

import { IconLabelData } from "@/common/types/IconLabelData";

import { LogoKey } from "../keys/LogoKey";
import { LOGO_TAG_MAP } from "../maps/LogoTagMap";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export function toIconLabelDataArray(logoKeyArray: LogoKey[]): IconLabelData[] {
  return logoKeyArray.map((logoKey) => (LOGO_TAG_MAP[logoKey]));
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
