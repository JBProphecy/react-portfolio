////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export type TransitionTimingFunction =
| `cubic-bezier(${number}, ${number}, ${number}, ${number})`
| "ease"
| "ease-in"
| "ease-in-out"
| "ease-out"
| "inherit"
| "initial"
| "linear"
| "step-end"
| "step-start"
| `steps(${number})`
| `steps(${number}, end)`
| `steps(${number}, start)`
| "unset";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
