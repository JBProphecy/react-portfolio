////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

import { createContext } from "react"

import { ProjectKey } from "@/data/PROJECT_MAP";
import { SectionKey } from "@/data/SECTION_MAP";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export type AppContextType = {
  activeSectionKey: SectionKey;
  setActiveSectionKey: React.Dispatch<React.SetStateAction<SectionKey>>;

  currentProjectKey: ProjectKey | null;
  setCurrentProjectKey: React.Dispatch<React.SetStateAction<ProjectKey | null>>;

  isProjectModalOpen: boolean;
  openProjectModal: () => void;
  closeProjectModal: () => void;
  toggleProjectModal: () => void;
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export const AppContext = createContext<AppContextType | undefined>(undefined)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
